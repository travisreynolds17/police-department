{
  "name": "fitness",
  "version": "1.0.0",
  "description": "Basic fitness-helper web app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass --watch scss --o dist/css --recursive"
  },
  "author": "Travis Reynolds",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "node-sass": "^4.11.0"
  }
}
